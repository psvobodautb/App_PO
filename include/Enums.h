#pragma once

enum Language {
    Czech,
    English
};

enum StudyType {
    Fulltime,
    Combined
};

enum StudyYear {
    First,
    Second,
    Third,
    Fourth,
    Fifth
};

enum Semester {
    Summer,
    Winter
};

enum Ending {
    Credit,
    ClassifiedCredit,
    Exam
};

enum StudyForm {
    Bachelor,
    Master,
    Doctoral
};